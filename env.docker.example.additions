# Add these to your existing .env -- generate each secret with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

POSTGRES_PASSWORD=
JWT_SECRET=

# DATABASE_URL is set directly in docker-compose.yml's environment: block
# (same reasoning as REDIS_URL) -- don't set it here, it would be ignored.
